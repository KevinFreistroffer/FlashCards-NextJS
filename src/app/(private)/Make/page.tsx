import React from "react";

interface MakeProps {
  // Define the props for the Make component here
}

const Make: React.FC<MakeProps> = () => {
  // Implement the Make component logic here

  return (
    <div>
      <p>Question formatting is not available at this time.</p>
    </div>
  );
};

export default Make;
